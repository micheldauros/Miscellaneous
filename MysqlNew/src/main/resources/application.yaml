spring:
  datasource:
    driver-class-name: ${local.driver-class-name}
    url: ${local.url}
    password: ${local.password}
    username: ${local.username}
  config:
    import: "local.yaml"
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl




#local:
#  driver-class-name: com.mysql.cj.jdbc.Driver
#  url: jdbc:mysql://localhost:3306/sql_training?characterEncoding=utf8&useSSL=false&serverTimezone=UTC&rewriteBatchedStatements=true
#  username: root
#  password: Unevensea856
